# Kalkulator-with-MSTests

It's very simple calculator ,created using Windows Forms.

In the second poroject named "KalkulatorTests" i made simple unit tests using MsTests, which checks if functions works properly(maths).
